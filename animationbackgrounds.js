  //unoptimized
  document.addEventListener('DOMContentLoaded', function() {
    const btn = document.getElementById('investing-btn');
    const tile = document.getElementById('firstTile');
    btn.addEventListener('mouseover', function() {
      tile.classList.add('opacity-in');
      tile.classList.remove('opacity-out');
    });
    btn.addEventListener('mouseout', function() {
      tile.classList.remove('opacity-in');
      tile.classList.add('opacity-out');
    });
  });
  document.addEventListener('DOMContentLoaded', function() {
    const btn = document.getElementById('fafsa-btn');
    const tile = document.getElementById('secondTile');
    btn.addEventListener('mouseover', function() {
      tile.classList.add('opacity-in');
      tile.classList.remove('opacity-out');
    });
    btn.addEventListener('mouseout', function() {
      tile.classList.remove('opacity-in');
      tile.classList.add('opacity-out');
    });
  });
  document.addEventListener('DOMContentLoaded', function() {
    const btn = document.getElementById('college-btn');
    const tile = document.getElementById('thirdTile');
    btn.addEventListener('mouseover', function() {
      tile.classList.add('opacity-in');
      tile.classList.remove('opacity-out');
    });
    btn.addEventListener('mouseout', function() {
      tile.classList.remove('opacity-in');
      tile.classList.add('opacity-out');
    });
  });

  document.getElementById('fourthTile').classList.add('opacity-out');
  document.addEventListener('DOMContentLoaded', function() {

    const btn = document.getElementById('employment-btn');
    const tile = document.getElementById('fourthTile');
    btn.addEventListener('mouseover', function() {
      tile.classList.add('opacity-in');
      tile.classList.remove('opacity-out');
    });
    btn.addEventListener('mouseout', function() {
      tile.classList.remove('opacity-in');
      tile.classList.add('opacity-out');
    });
  });

  document.getElementById('sixthTile').classList.add('opacity-out');
  document.addEventListener('DOMContentLoaded', function() {
    const btn = document.getElementById('banking-btn');
    const tile = document.getElementById('sixthTile');
    btn.addEventListener('mouseover', function() {
      tile.classList.add('opacity-in');
      tile.classList.remove('opacity-out');
    });
    btn.addEventListener('mouseout', function() {
      tile.classList.remove('opacity-in');
      tile.classList.add('opacity-out');
    });
  });
  document.getElementById('seventhTile').classList.add('opacity-out');
  document.addEventListener('DOMContentLoaded', function() {
    const btn = document.getElementById('taxes-btn');
    const tile = document.getElementById('seventhTile');
    btn.addEventListener('mouseover', function() {
      tile.classList.add('opacity-in');
      tile.classList.remove('opacity-out');
    });
    btn.addEventListener('mouseout', function() {
      tile.classList.remove('opacity-in');
      tile.classList.add('opacity-out');
    });
  });
  document.getElementById('eighthTile').classList.add('opacity-out');
  document.addEventListener('DOMContentLoaded', function() {
    const btn = document.getElementById('budget-btn');
    const tile = document.getElementById('eighthTile');
    btn.addEventListener('mouseover', function() {
      tile.classList.add('opacity-in');
      tile.classList.remove('opacity-out');
    });
    btn.addEventListener('mouseout', function() {
      tile.classList.remove('opacity-in');
      tile.classList.add('opacity-out');
    });
  });
  document.addEventListener('DOMContentLoaded', function() {
    const btn = document.getElementById('psa-btn');
    const tile = document.getElementById('ninthTile');
    btn.addEventListener('mouseover', function() {
      tile.classList.add('opacity-in');
      tile.classList.remove('opacity-out');
    });
    btn.addEventListener('mouseout', function() {
      tile.classList.remove('opacity-in');
      tile.classList.add('opacity-out');
    });
  });



  